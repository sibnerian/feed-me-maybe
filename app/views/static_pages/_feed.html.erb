<% if @feed_items.any? %>
    <%= will_paginate @feed_items %>
    <ol class="events" style="text-align: center;">
      <%= render partial: 'feed_item', collection: @feed_items %>
    </ol>
    <%= will_paginate @feed_items %>
<% end %>